import {
  drawerWidth,
  transition,
  container
} from "assets/jss/styles/index.js";

const landingStyle = theme => ({
  landingWrapper: {
    position: "relative",
    minHeight: "100vh"
  }
});

export default landingStyle;
